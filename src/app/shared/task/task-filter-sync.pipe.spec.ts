import { TaskFilterSyncPipe } from './task-filter-sync.pipe';

describe('TaskFilterSyncPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskFilterSyncPipe();
    expect(pipe).toBeTruthy();
  });
});
